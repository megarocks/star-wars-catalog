import { css } from 'lit-element';

export const entityStyles = css`
  .entity {
    margin: 3rem 0;
  }
  h3 {
    font-weight: 500;
  }
  label {
    float: left;
    font-weight: 500;
    margin-right: 1rem;
  }
  .spec {
    margin-bottom: 0.5rem;
    line-height: 1.75rem;
  }
  sw-resource-item-link {
    margin-right: 0.5rem;
  }
`;
